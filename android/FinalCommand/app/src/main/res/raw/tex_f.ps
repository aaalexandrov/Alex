precision mediump float;

uniform sampler2D sTexture;
uniform vec4 uColor;  

varying vec2 vTextureCoord;

void main() {
  gl_FragColor = vec4(uColor.rgb, texture2D(sTexture, vTextureCoord).a * uColor.a);
}
